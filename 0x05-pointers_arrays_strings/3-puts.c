#include "main.h"
#include <string.h>
#include <stdio.h>

/**
  * _puts - function that prints a string, followed by a new line, to stdout.
  * @str: string to print
  * Return: void
  */

void _puts(char *str)
{
	puts(str);
	puts('\n');
}
