#include "main.h"
#include <stdio.h>

/**
  * _strlen - function that returns the length of a string.
  * @s: a string
  * Return: returns the length of a string.
  */

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);

}
